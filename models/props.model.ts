export interface LoaderProps {
    titleLoader: string
}

export interface AppTitleProps {
    title: string
    slogan: string
    imageUrl: string
}

export interface ProductDetailPageProps {
    params: {
        productTitle: string;
    };
}